{{!-- TODO: Only if the user is logged in, render the painting content --}}
<section class="painting">
  <img src="/images/{{painting.filename}}" alt="{{painting.filename}}">
  <div class="painting-details">
    <h1>{{painting.title}}</h1>
    <p class="text-center">Artist: {{painting.artist}}</p>
    <p>Exhibition open until: {{ format_date painting.date}}</p>
  </div>
</section>
{{!-- TODO: Otherwise, provide a link to login --}}